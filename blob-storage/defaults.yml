parameters:
- name: project_name
  type: string
- name: environment
  type: string

# Defines Blob Srtorage Name
variables:
  - name: blob_name
    value: "sa${{ parameters.project_name }}blb${{ parameters.environment }}"